# AGENTS.md

本文件记录本项目中 AI 助手与用户协作时必须遵守的开发环境、路径规范、实验流程和 Git 工作流约定。后续在本仓库内工作时，应优先遵守本文档。

## 开发环境约定

- 用户在本地 macOS 机器上使用 IDE（PyCharm）浏览、修改和提交代码。
- 本地项目目录为：`/Users/denghaoyuan/workspace/MyCode/multi-lane-main`。
- 代码运行、模型训练和实验执行发生在远程 Linux 服务器上；服务器项目目录为：
  `/mnt/haoyuan/workspace/multi-lane-main`。
- 本地和服务器是两个独立 Git 工作树，各自维护独立的 `.git`，共同跟踪
  `git@github.com:denghy12/multi-lane.git`。
- Git 是受版本控制代码的唯一同步和版本来源；禁止通过 SFTP 复制、覆盖或删除任一端的
  `.git/`。
- PyCharm Automatic Upload 默认保持关闭。SFTP 只用于按需传输数据集、日志、输出、
  checkpoint 等未纳入 Git 的实验产物，不自动上传受 Git 管理的代码。

## 编码与路径规范

- 始终把当前代码库视为运行在 Linux 环境下。
- 涉及 Shell 命令、系统环境、文件权限、进程管理、训练脚本时，使用 Linux 标准写法。
- 代码中不得写入 Windows 风格绝对路径，例如 `D:\...` 或 `C:\...`。
- 读取数据集、保存模型、保存日志、保存实验输出时，优先使用基于项目根目录的相对路径。
- 当需要说明远程服务器上的项目位置、进入项目目录或给出远程执行命令时，可以使用服务器项目目录：`/mnt/haoyuan/workspace/multi-lane-main`。
- 推荐路径：
  - 数据集：优先使用 `./datasets/`；如历史命令或旧代码使用 `./data/`，应先确认实际数据是否存在。
  - 模型权重与检查点：`./models/` 或项目已有的权重目录
  - 实验日志：`./logs/`
  - 实验结果：`./output/`
- 如确实需要绝对路径，应使用 Linux 路径，并先向用户确认。

## Git 工作流

- 每次修改新的模块前，不要直接在 `main` 分支上修改。
- 开始修改前，应先创建新的实验或功能分支。
- 分支命名建议：
  - `exp/<short-topic>`
  - `fix/<short-topic>`
  - `feature/<short-topic>`
- 修改完成并初步验证后，先总结改动和效果。
- 如果代码实现效果好，应询问用户是否提交到 Git 并推送到远端仓库。
- 只有当实验效果好并经用户确认后，才考虑合并到 `main` 分支。
- 标准同步流程：本地修改并检查 `git status`/`git diff`，在功能或实验分支提交并 push；
  服务器执行 `git fetch origin`，切换到同一分支或 commit，并只使用 fast-forward 更新。
- 服务器更新已检出的分支时优先使用 `git pull --ff-only`，禁止用 SFTP 让服务器工作树
  提前出现同一批受控代码修改。
- 两端需要复现实验时，应记录并核对相同的 branch、HEAD 和 upstream；不得复制 `.git`
  来“同步”仓库。
- 回到历史版本时，优先从目标 commit 创建恢复/实验分支，再让两端 fetch 后切换到该分支；
  不使用 `git reset --hard` 或 `git clean` 清除当前工作。
- 服务器额外 worktree `/mnt/haoyuan/workspace/multi-lane-main-test-only` 必须保留；操作分支前
  应先运行 `git worktree list` 并分别检查各 worktree 状态。

## 上下文文档维护规则

项目根目录下维护三个上下文文档：

- `PROJECT_CONTEXT.md`
- `WORK_LOG.md`
- `TODO_NEXT.md`

每次发生以下任一变化后，必须同步检查并更新上述文档：

- 修改业务代码、配置文件、训练脚本或新增实验分支。
- 新增、删除、stash、提交或合并实验代码。
- 运行新的 smoke/full 实验并产生新的日志或输出。
- 确认新的 Git 状态、实验结果、风险点或下一步方向。
- 修改本文件中的协作规则。

如果某次修改不需要更新其中某个上下文文档，应在回复中明确说明原因。

## 实验流程

每次开始实验前，必须先告知用户本次实验的完整配置，包括但不限于：

- 实验目标
- 使用的数据集
- 模型或方法名称
- 训练脚本或入口命令
- 关键超参数，例如 batch size、learning rate、epoch、seed、optimizer、scheduler 等
- 输入路径、输出路径、日志路径
- 使用的 checkpoint 或 pretrained 权重
- 预期产物

实验运行要求：

- 日志输出到 `./logs/`。
- 实验结果输出到 `./output/`。
- 不要把临时结果散落到项目根目录。
- 如新增脚本或配置，应尽量让其可复现，并使用相对路径。

实验结束后：

- 提醒用户将远程实验结果同步回本地。
- 在用户同步结果后，帮助分析 `./logs/` 和 `./output/` 中的实验结果。
- 分析内容应包括关键指标、异常现象、可能原因、下一步改进建议。

## 与用户沟通

- 修改前简要说明将要改动的文件和目的。
- 修改后总结：
  - 改了哪些文件
  - 实现了什么功能或修复了什么问题
  - 是否运行了验证
  - 是否需要用户在远程 Linux 环境中执行训练或测试
- 当涉及实验、Git 提交、推送、合并或删除数据时，默认必须先向用户确认；下述持续授权范围除外。

## 实验执行持续授权

- 用户于 2026-09-03 明确授权：当用户已经要求“开始/执行”某个下一步实验时，无需再次确认，
  可直接在实验分支完成提交与推送、通过 Git-only 流程在服务器创建或同步独立 worktree、运行
  已声明的单元测试与 smoke，并在检查通过后启动该次已声明的非破坏性实验。
- 该授权不包括：合并到 `main`、删除数据或 checkpoint、覆盖任一工作树中的未提交用户改动、
  修改 `/mnt/haoyuan/workspace/multi-lane-main-test-only`，以及其他破坏性或不可恢复操作；这些操作
  仍须单独确认。
